services:
  postgres:
    image: postgres:16-alpine
    container_name: ${COTERM_DB_CONTAINER_NAME}
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${COTERM_DB_NAME}
      POSTGRES_PASSWORD: ${COTERM_DB_PASSWORD}
      POSTGRES_USER: ${COTERM_DB_USER}
    ports:
      - "${COTERM_DB_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${COTERM_DB_USER} -d ${COTERM_DB_NAME}"]
      interval: 1s
      timeout: 5s
      retries: 30
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
    name: ${COTERM_DB_VOLUME_NAME}
