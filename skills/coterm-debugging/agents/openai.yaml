interface:
  display_name: "coterm Debugging"
  short_description: "Use coterm debug logs, Debug menu, and runtime pitfall rules safely."
  default_prompt: "Use this skill when adding debug probes, diagnosing coterm UI/runtime issues, touching typing-sensitive terminal paths, SwiftUI list rows, drag/drop UTTypes, search overlay layering, or Debug menu windows."
