interface:
  display_name: "coterm Release"
  short_description: "Prepare coterm releases with version, changelog, guard, tag, and asset rules."
  default_prompt: "Use this skill when preparing a coterm release, bumping versions, updating CHANGELOG.md, running the release pretag guard, tagging, or troubleshooting release assets."
