interface:
  display_name: "coterm Shared Behavior"
  short_description: "Keep multi-entrypoint behavior and optimistic updates on one shared path."
  default_prompt: "Use this skill when changing behavior exposed through multiple coterm entrypoints, optimistic UI or CLI mutation flows, or tests that previously missed a user-visible bug."
